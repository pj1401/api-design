[project]
name = "tracks-api"
version = "2026.0.0"
requires-python = ">=3.12"
dependencies = [
    "bcrypt>=5.0.0",
    "dotenv>=0.9.9",
    "flask>=3.1.3",
    "flask-jwt-extended>=4.7.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]
